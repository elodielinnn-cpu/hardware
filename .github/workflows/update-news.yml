name: Update news data

on:
  workflow_dispatch:
  schedule:
    # 00:00 UTC = 08:00 Asia/Shanghai
    - cron: "0 0 * * *"

permissions:
  contents: write

jobs:
  update-news:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Fetch latest news
        run: node scripts/fetch_real_sources.mjs

      - name: Sync static publish directory
        run: |
          cp real-data.js taxonomy.js site/

      - name: Commit updated data
        run: |
          if git diff --quiet; then
            echo "No news data changes to commit."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add real-data.js taxonomy.js site/real-data.js site/taxonomy.js
          git commit -m "Update news data"
          git push

